[header]

const inherit = require('pdl/infrastructure/languages/js/inheritance').inherit;

/**
 * @class {[class-name]}
 *
[jsdoc-properties]
 */
const [class-name] = function () {

    this.__type = '[class-declaration]';
    [const-list]
    [method-list]
};

/** inheritance */
inherit( [namespace], '[class-name]', [full-parent-class] );

[property-attributes]

/**
[jsdoc-class]
 *
[jsdoc-properties]
 */
[class-declaration] = [class-name];

export default [class-name];
